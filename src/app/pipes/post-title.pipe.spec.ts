import { PostTitlePipe } from './post-title.pipe';

describe('PostTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PostTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
